-- use CNX
-- GO
-- insert into RequestType_lkp (RequestTypeId, Name, Currency)
-- select RequestTypeId + 22, REPLACE(Name, 'TNB', 'TRIA') as Name, Currency from RequestType_lkp where Name like '%TNB%'

-- insert into RequestType_lkp (RequestTypeId, Name, Currency)
-- select RequestTypeId + 24, REPLACE(Name, 'TNB', 'BEZ') as Name, Currency from RequestType_lkp where Name like '%TNB%'

-- insert into RequestType_lkp (RequestTypeId, Name, Currency)
-- select RequestTypeId + 26, REPLACE(Name, 'TNB', 'SLRM') as Name, Currency from RequestType_lkp where Name like '%TNB%'
